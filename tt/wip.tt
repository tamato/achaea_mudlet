#nop test scripts


#class wip kill
#class wip open

#alias {add}
{
   #local ply %1;

   addSeenPlayer <070>$ply<099>;

   #list seenPlys ins -1 $ply;
}

#alias pvp {
   #var pcnt &seenPlys[];
   #if {%1 > $pcnt} {dbg {Invalid PvP index}; #return};

   #show Target is: $seenPlys[%1];
   #send {emote wave's at $seenPlys[%1]};
}

#class wip save
#class wip close
